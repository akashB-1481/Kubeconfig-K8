apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: nginx-ns
spec:
  selector:
    app: nginx-app
  type: LoadBalancer

  ports:
    - name: nginx-port
      protocol: TCP
      port: 80
      targetPort: 80
